services:
  - type: static
    name: receiptor-pro
    repo: https://github.com/<your-org-or-user>/<repo>
    branch: main
    buildCommand: npm run build
    publishPath: build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
